import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      paddingTop: '8%',
      borderRadius: 35,
      position: 'absolute',
      top: '20%',
      backgroundColor: '#fff',
      width: '100%'
    }
});
